import { FC } from 'react';
import classes from './NotFound.module.css';

const NotFound: FC = () => {
  return <div className={classes.notFoundContainer}>Page is not found 🐈</div>;
};

export default NotFound;
